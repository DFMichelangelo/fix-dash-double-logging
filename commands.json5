{
    // Questo è il comando per avviare l'applicazione
    "start": "echo 'Starting the app...'",

    // Questo comando costruisce l'applicazione
    "build": "echo 'Building the app...'",

    // Esegue i test
    "test": "echo 'Running tests...'",

    // Esegue il deploy
    "deploy": "echo 'Deploying the app...'"
}
